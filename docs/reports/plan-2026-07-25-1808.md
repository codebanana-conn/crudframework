# Plan — KskPhieuForm: Fix controls not displaying (Final Pass)

**Thời gian:** 2026-07-25 18:08 (Asia/Bangkok)

---

## 1. Yêu cầu

Tiếp tục sửa lỗi `KskPhieuForm` — các editor control không hiển thị trên form.

## 2. Kế hoạch & Kết quả

### Hạng mục 1: Xóa `AddTongHopControlsToTab()` method + call
- **Vấn đề:** `BuildTongHopTab()` cuối cùng gọi `AddTongHopControlsToTab()` để `Controls.Add` vào `tabTongHop`, nhưng Designer.cs giờ đã làm điều này.
- **Cách giải quyết:** Xóa method definition + call site khỏi `KskPhieuForm.cs`.
- **Kết quả:** ✅ Đã xóa.
- **Trạng thái:** ✅

### Hạng mục 2: Xóa `AddTreEmControlsToTab()` method + call
- **Vấn đề:** Tương tự trên, `BuildTreEmTab()` gọi `AddTreEmControlsToTab()`.
- **Cách giải quyết:** Xóa method definition + call site.
- **Kết quả:** ✅ Đã xóa.
- **Trạng thái:** ✅

### Hạng mục 3: Xóa `tab6_18.Controls.AddRange(...)` trong `Build6_18Tab()`
- **Vấn đề:** Designer.cs đã thêm controls vào `tab6_18`, code-behind thêm lần nữa → duplicate.
- **Cách giải quyết:** Xóa block `tab6_18.Controls.AddRange(...)` cuối method.
- **Kết quả:** ✅
- **Trạng thái:** ✅

### Hạng mục 4: Xóa `tab18.Controls.AddRange(...)` trong `Build18Tab()`
- **Vấn đề:** Tương tự — Designer.cs đã xử lý.
- **Cách giải quyết:** Xóa block `tab18.Controls.AddRange(...)`.
- **Kết quả:** ✅
- **Trạng thái:** ✅

### Hạng mục 5: Xóa `tabTamThan.Controls.AddRange(...)` trong `BuildTamThanTab()`
- **Vấn đề:** Tương tự — Designer.cs đã xử lý.
- **Cách giải quyết:** Xóa block `tabTamThan.Controls.AddRange(...)`.
- **Kết quả:** ✅
- **Trạng thái:** ✅

### Hạng mục 6: Xóa import `DevExpress.XtraTab` không cần thiết
- **Vấn đề:** `KskPhieuForm.cs` import `DevExpress.XtraTab` nhưng không còn dùng `XtraTabPage` trong code-behind.
- **Cách giải quyết:** Xóa dòng `using DevExpress.XtraTab;`.
- **Kết quả:** ✅
- **Trạng thái:** ✅

### Hạng mục 7: Kiểm tra `check-all.sh`
- **Vấn đề:** Đảm bảo không lỗi biên dịch hoặc cú pháp.
- **Cách giải quyết:** Chạy `tools/check-all.sh`.
- **Kết quả:** ✅ Core build PASS, Syntax check PASS (41 files, 0 lỗi).
- **Trạng thái:** ✅

### Hạng mục 8: Audit control hierarchy Designer.cs
- **Vấn đề:** Đảm bảo 242 fields đầy đủ, không duplicate, layout đúng thứ tự.
- **Cách giải quyết:** Agent audit qua 7 tiêu chí:
  1. Mỗi `private` field có `new` trong `InitializeComponent()` → ✅ 242/242
  2. Mỗi control `new` đều có parent container → ✅
  3. Không duplicate `Controls.Add` → ✅
  4. 5 tab pages trong `tabKsk.TabPages` → ✅
  5. `SuspendLayout` → `new` → `Controls.Add` → `ResumeLayout` → `PerformLayout` → ✅
  6. `tabKsk.Dock = DockStyle.Fill` → ✅
  7. Tab page `.Text` đã set → ✅
- **Kết quả:** ✅ All 7 checks PASS.
- **Trạng thái:** ✅

## 3. Nhật ký thực hiện

- 18:08 — Bắt đầu xóa các `Add*ControlsToTab` methods và duplicate `Controls.AddRange` trong code-behind.
- 18:09 — Hoàn tất xóa, chạy `check-all.sh` → PASS.
- 18:10 — Audit control hierarchy qua agent → 7/7 checks PASS.
