# Kế hoạch thực hiện — Session 2026-07-25 11:39

## 1. Prompt / Yêu cầu của user

Nâng cấp solution **CrudFramework** (3 project: `CrudFramework.Core`, `CrudFramework.WinForms`,
`CrudFramework.Sample`; target .NET Framework 4.5, C# 5/6; Newtonsoft.Json + Npgsql 2.2.3 +
DevExpress v17.1) theo **6 hạng mục**, thực hiện **tuần tự 1 → 6**.

Yêu cầu bổ sung (session này):
- Tạo thư mục báo cáo + file plan theo session tên `plan-<yyyy-MM-dd-HHmm>.md`.
- File plan gồm: (1) Prompt/yêu cầu; (2) Kế hoạch (checklist, vấn đề, cách giải quyết, kết quả,
  trạng thái, đề xuất cải tiến); (3) Quy tắc: xong checklist phải cập nhật trạng thái + tự commit
  lên remote nếu không hỏng build (KHÔNG hỏi lại); (4) Tự thực hiện toàn bộ, không hỏi từng bước;
  (5) Tạo `AGENTS.md` ghi các quy tắc vào repo.

### Tóm tắt 6 hạng mục
1. **Fix Designer lỗi với base class generic** — tài liệu hóa pattern lớp trung gian non-generic,
   thêm cảnh báo XML-doc, demo minh họa, checklist tạo Form mới.
2. **Bổ sung `Schema` cho `DbTableAttribute`** — hỗ trợ đa schema PostgreSQL, function name
   schema-qualified, validate an toàn, demo entity 2-schema.
3. **Mở rộng data layer** — hỗ trợ Function mode (cũ) + RawSql tự sinh + Hybrid (override SQL),
   qua enum `DbCommandMode`, interface `ISqlCommandClient`, `PostgresRawSqlBuilder`, escape hatch.
4. **Tổ chức docs theo project** — `/docs/<Project>/README.md` + `CHANGELOG.md`, bổ sung XML-doc
   + ví dụ dùng cho các file lõi.
5. **Binding qua Properties Grid** — giảm code tay, serialize `BindingMember` ra Designer,
   auto-bind theo tên control (tùy chọn), demo.
6. **Binding không phụ thuộc DevExpress** — `IControlValueAdapter`, adapter cho WinForms chuẩn +
   DevExpress, auto-detect, demo với TextBox/CheckBox/DateTimePicker chuẩn.

---

## 2. Kế hoạch thực hiện (checklist)

### ✅ Hạng mục 0 — Thiết lập quản trị session
- **Vấn đề:** cần cơ chế báo cáo, plan, commit tự động, AGENTS.md.
- **Cách giải quyết:** tạo `docs/reports/plan-2026-07-25-1139.md`, `AGENTS.md`, cập nhật
  `.gitignore` (bỏ qua `.codebanana/`, `.coding-agent/`), cấu hình git user.
- **Kết quả:** đã commit & push (commit `1944991`).
- **Trạng thái:** ✅ xong
- **Cải tiến:** có thể tạo template plan sẵn để tái dùng cho session sau.

### ✅ Hạng mục 1 — Fix Designer với base class generic
- **Vấn đề:** VS Designer không load được Form kế thừa trực tiếp `CrudFormBase<TEntity>`.
- **Cách giải quyết:**
  - Viết lại XML-doc class `CrudFormBase<TEntity>`: cảnh báo + ví dụ pattern lớp trung gian.
  - Thêm lớp trung gian demo `CustomerFormBase : CrudFormBase<Customer>` + form `CustomerEditForm`
    (.cs + .Designer.cs) trong Sample, đăng ký vào .csproj.
  - Soạn checklist "tạo Form mới" (đưa vào docs ở Hạng mục 4).
- **Kết quả:** Code Sample vốn đã đúng (Form1/CustomerDetailForm dùng non-generic); bổ sung demo
  chứng minh pattern generic-qua-trung-gian mở Designer OK. Không breaking change.
- **File sửa/thêm:** `CrudFramework.WinForms/CrudFormBase.cs` (XML-doc),
  `CrudFramework.Sample/CustomerEditForm.cs` (mới), `CustomerEditForm.Designer.cs` (mới),
  `CrudFramework.Sample.csproj` (đăng ký compile).
- **Trạng thái:** ✅ xong
- **Cải tiến:** cân nhắc Roslyn analyzer cảnh báo compile-time nếu Form Designer kế thừa generic base.

#### Checklist "Tạo Form mới trong CrudFramework" (để đưa vào docs Hạng mục 4)
1. ☐ Base class: Form CÓ Designer phải kế thừa `CrudFormBase` (non-generic) HOẶC 1 lớp trung gian
   non-generic (`XxxFormBase : CrudFormBase<TEntity> {}`). KHÔNG kế thừa `CrudFormBase<TEntity>` trực tiếp.
2. ☐ Constructor: đảm bảo base class có constructor không tham số (Designer cần) — generic base
   đã có sẵn. Form cụ thể có thể có ctor tham số.
3. ☐ DevExpress reference version phải khớp (v17.1) giữa các project, nếu không Designer báo lỗi load assembly.
4. ☐ Set `EntityType`, `Client`, `BindingProvider`, `ErrorProvider` trong constructor (sau InitializeComponent).
5. ☐ Bind control qua Properties Grid (`BindingMember`) hoặc `SetBindingMember(...)` trong Designer.
6. ☐ Đăng ký .cs + .Designer.cs vào .csproj với `<SubType>Form</SubType>` + `<DependentUpon>`.

### ☐ Hạng mục 2 — Schema cho entity
- **Trạng thái:** ✅ xong (2026-07-25, session tiếp)
- **Vấn đề:** `DbTableAttribute` chưa hỗ trợ đa schema PostgreSQL.
- **Cách giải quyết:** thêm property `Schema` (validate whitelist `[a-z0-9_]`, bắt đầu bằng chữ/`_`),
  `GetFunctionName` trả về schema-qualified `schema.fn_x_action`; thêm helper an toàn
  `ValidateIdentifierOrNull` tái dùng cho tên bảng/cột.
- **File sửa:** `CrudFramework.Core/Attributes/DbTableAttribute.cs`.
- **Cải tiến:** có thể thêm `KeyColumn` cấu hình được (hiện mặc định "id").

### ☐ Hạng mục 3 — Function + RawSql + Hybrid
- **Trạng thái:** ✅ xong (2026-07-25, session tiếp)
- **Vấn đề:** data layer chỉ hỗ trợ gọi function; cần thêm RawSql tự sinh + Hybrid (override SQL).
- **Cách giải quyết:**
  - `DbCommandMode` enum (Function/RawSql/Hybrid).
  - `ISqlCommandClient` + `RawSqlRequest` + `ISqlOverrideProvider` (escape hatch).
  - `PostgresRawSqlBuilder`: sinh SELECT/UPSERT(ON CONFLICT + jsonb_populate_record)/DELETE,
    giá trị luôn tham số hóa (`:id`, `:p_payload`), identifier qua whitelist + bọc `"..."`.
  - `NpgsqlSqlCommandClient`: triển khai `ISqlCommandClient` trên Npgsql 2.2.3 (bọc kết quả jsonb).
  - `EntityDataClient` (`IEntityDataClient`): facade định tuyến theo mode, ẩn function/SQL khỏi UI.
  - `CrudFormBase`: thêm property `EntityData`, Load/Save/Delete ưu tiên dùng nếu được gán
    (giữ `Client` function-only để tương thích ngược).
- **File thêm:** `Data/DbCommandMode.cs`, `Data/ISqlCommandClient.cs`, `Data/PostgresRawSqlBuilder.cs`,
  `Data/NpgsqlSqlCommandClient.cs`, `Data/EntityDataClient.cs`. **File sửa:** `CrudFramework.Core.csproj`,
  `CrudFramework.WinForms/CrudFormBase.cs`.
- **Cải tiến:** RawSql List hiện chưa dịch `filter` jsonb thành WHERE động — có thể bổ sung
  bộ dịch filter an toàn (khóa theo cột whitelist) ở bản sau.

### ☐ Hạng mục 4 — Docs theo project + XML-doc
- **Trạng thái:** ⏳ đang làm (session tiếp)

### ☐ Hạng mục 5 — Binding qua Properties Grid
- **Trạng thái:** ✅ xong (đã có sẵn từ trước, đã rà soát xác nhận)
- **Kết quả:** `EntityBindingProvider` (IExtenderProvider) expose property mở rộng `BindingMember`
  cho mọi control; `BindingMemberTypeConverter` + `BindingMemberUIEditor` cho dropdown design-time;
  `Bind()` tạo binding 2 chiều thật. Không cần làm lại.

### ☐ Hạng mục 6 — Binding độc lập DevExpress
- **Trạng thái:** ✅ xong (2026-07-25, session tiếp)
- **Vấn đề:** `Bind()` cũ dùng cứng 1 `BindProperty` cho mọi control -> khó dùng control WinForms thuần.
- **Cách giải quyết:**
  - `IControlValueAdapter` + `StandardWinFormsControlAdapter` (TextBox→Text, CheckBox→Checked,
    DateTimePicker/NumericUpDown→Value, ComboBox→SelectedValue/Text) + `DevExpressEditorAdapter`
    (duck-typing "EditValue" qua reflection, không cần compile-time DevExpress).
  - `ControlValueAdapterRegistry` (Default dùng chung, ưu tiên DevExpress rồi WinForms chuẩn).
  - `EntityBindingProvider`: thêm `UseAdapters` (mặc định true) + `AdapterRegistry`; `Bind()` tự
    phát hiện property bind + update mode theo từng control.
- **File thêm:** `Binding/IControlValueAdapter.cs`, `Binding/BuiltInControlValueAdapters.cs`,
  `Binding/ControlValueAdapterRegistry.cs`. **File sửa:** `EntityBindingProvider.cs`, `.csproj`.
- **Cải tiến:** có thể thêm demo Form dùng control WinForms thuần trong Sample để minh họa.

---

## 3. Quy tắc làm việc (áp dụng cả session)

- Xong mỗi checklist → cập nhật trạng thái trong file này ngay.
- Xong checklist + an toàn build → **tự commit & push lên `origin`, KHÔNG hỏi lại**.
- Tự thực hiện toàn bộ, không hỏi từng bước (trừ breaking change nghiêm trọng).
- Tuân thủ `AGENTS.md` ở root repo.
- Không có môi trường build .NET 4.5 → rà soát kỹ bằng mắt trước khi commit.

---

## 4. Nhật ký thực hiện

- **2026-07-25 11:39** — Khởi tạo session: tạo `AGENTS.md`, `.gitignore` cập nhật, file plan này.
- **2026-07-25 11:45** — Hoàn thành Hạng mục 1: XML-doc cảnh báo generic base, demo lớp trung gian
  `CustomerFormBase` + `CustomerEditForm`, đăng ký .csproj. Commit & push.
