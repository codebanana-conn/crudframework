# Plan — Bổ sung Form "Khám sức khỏe theo TT 25/2026/TT-BYT" vào CrudFramework.Sample

- **Ngày lập:** 2026-07-25 16:52 (Asia/Bangkok)
- **Nguồn yêu cầu:** `issue/prompt.md` + tài liệu nghiệp vụ `issue/Thong-mo-ta-bo-sung-chuc-nang-mau-giay-ksk-tt25-2026.md`

---

## 1. Prompt / Yêu cầu của user

> Hãy triển khai: `issue/prompt.md`

Tóm tắt yêu cầu (theo prompt.md):
- Bổ sung mẫu Khám sức khỏe (KSK) theo TT 25/2026/TT-BYT gồm 4 loại mẫu dùng chung 1 bảng:
  trẻ em <6 tuổi, 6–<18 tuổi, ≥18 tuổi, khám tâm thần.
- SQL demo idempotent (`sql/03_kham_suc_khoe.sql`) theo convention `01_customers.sql`, schema `public`.
- Entity `KskPhieu.cs`, Form `KskPhieuForm` (XtraTabControl: tab tổng hợp + tab con theo loại khám),
  nút "In phiếu kết quả" dạng split-button nhiều mẫu in (placeholder).
- Data layer `DbCommandMode.Function`.
- Cập nhật docs (`developments.md` mục 8, Sample README + CHANGELOG), chạy `tools/check-all.sh`,
  commit từng hạng mục (tiếng Việt, không hỏi lại).

**Ảnh mẫu bắt buộc:** `https://i.vgy.me/F39mfC.png` — ĐÃ tải thành công về `issue/assets/F39mfC.png`
và phân tích: form khám sức khỏe hợp đồng, annotation yêu cầu (1) chia tab tổng hợp + tab con
theo loại khám, (2) thêm field còn thiếu, (3) nút "In phiếu kết quả" có nhiều tùy chọn mẫu in.

---

## 2. Kế hoạch thực hiện

### Hạng mục 1 — SQL `sql/03_kham_suc_khoe.sql`
- **Vấn đề:** Bảng gốc `current.pskhamsuckhoe` (200+ cột) không có trong repo demo.
- **Cách giải quyết:** Tạo `ksk_benhnhan` (rút gọn) + `ksk_phieu` (bảng chính, cột lõi + `loai_mau_ksk`).
  Thêm ~130 cột 4 nhóm bằng `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` từng câu riêng + `COMMENT ON COLUMN`.
  4 function `fn_ksk_phieu_get/list/upsert/delete` theo contract `fn_customers_*`. Data mẫu 2 dòng.
- **Kết quả:** File 796 dòng, 4 function, ~130 cột ALTER idempotent, dữ liệu mẫu 3 dòng.
- **Trạng thái:** ✅ xong
- **Cải tiến lần sau:** Có thể tách generator sinh cột từ bảng markdown để giảm lỗi tay.

### Hạng mục 2 — Entity `CrudFramework.Sample/KskPhieu.cs`
- **Cách giải quyết:** `KskPhieu : EntityBase`, `[DbTable("ksk_phieu", FunctionPrefix="fn_")]`,
  vùng comment `// ---- Trẻ em <6 tuổi ----` ... `// ---- Tâm thần ----`, property `LoaiMauKsk`.
- **Kết quả:** 1058 dòng, ~130 property gắn `[DbColumn]` + XML-doc tiếng Việt.
- **Trạng thái:** ✅ xong

### Hạng mục 3 — Form `KskPhieuForm` (+ Designer)
- **Cách giải quyết:** `CrudFormBase` non-generic, `XtraTabControl` (Tổng hợp + 4 tab con),
  `CheckEdit` cho numeric(1,0) 0/1, `ComboBoxEdit`/`ImageComboBoxItem` cho cột có bảng mã "0:..;1:..",
  `DropDownButton` split-button "In phiếu kết quả" (placeholder MessageBox). Show/hide tab con theo `LoaiMauKsk`.
- **Kết quả:** Form 5 tab, nút in 4 mẫu, show/hide tab theo loại mẫu, đăng ký csproj + DemoLauncher.
- **Trạng thái:** ✅ xong

### Hạng mục 4 — Data layer
- **Cách giải quyết:** `DbCommandMode.Function`, gán `Client` (IDbFunctionClient) như `ProductLookupForm`.
- **Trạng thái:** ✅ xong

### Hạng mục 5 — Docs
- **Cách giải quyết:** Thêm dòng bảng mục 8 `developments.md`, cập nhật Sample README + CHANGELOG, cập nhật plan.
- **Trạng thái:** ✅ xong

### Hạng mục 6 — Kiểm tra trước commit
- **Cách giải quyết:** `bash tools/check-all.sh` — Core PASS + syntax-check 41 file 0 lỗi.
- **Trạng thái:** ✅ xong

---

## 3. Nhật ký thực hiện

- 2026-07-25 16:52 — Đọc prompt + tài liệu nghiệp vụ + convention repo. Tải & phân tích ảnh mẫu
  (`issue/assets/F39mfC.png`) OK. Tạo plan file. Bắt đầu Hạng mục 1 (SQL).
- 2026-07-25 — HM1 (SQL 03_kham_suc_khoe.sql) ✅ commit `99cf3c4`.
- 2026-07-25 — HM2 (KskPhieu.cs entity) ✅ commit `3153c96`.
- 2026-07-25 — HM3+4 (KskPhieuForm + data layer Function mode) ✅ commit `634dcee`.
- 2026-07-25 — HM5 (docs: developments.md mục 8, Sample README + CHANGELOG) ✅.
- 2026-07-25 — HM6 (check-all.sh PASS — Core build thật + syntax-check 41 file, 0 lỗi) ✅.
  Hoàn thành toàn bộ yêu cầu `issue/prompt.md`.
